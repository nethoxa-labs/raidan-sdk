# Repository instructions

Follow `AGENTS.md` and the canonical `raidan-specs` contracts. Keep this module
limited to stable reusable Ethereum primitives. Network-worker behavior belongs
in `raidan-worker-ethereum`, and generic execution belongs in `raidan-core`.
Never rewrite or republish an existing SDK tag or release.
