package reqresp

const (
	// MetadataV1 is the consensus Metadata v1 protocol ID.
	MetadataV1 = ProtocolPrefix + "/metadata/1/" + Encoding
	// MetadataV2 is the consensus Metadata v2 protocol ID.
	MetadataV2 = ProtocolPrefix + "/metadata/2/" + Encoding
	// MetadataV3 is the consensus Metadata v3 protocol ID.
	MetadataV3 = ProtocolPrefix + "/metadata/3/" + Encoding
)
